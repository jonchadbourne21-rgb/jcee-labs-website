services:
  frameforge:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      PREVIS_DATABASE_PATH: /app/data/previs.db
    volumes:
      - frameforge-data:/app/data
    restart: unless-stopped

volumes:
  frameforge-data:
